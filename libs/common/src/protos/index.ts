import { join } from 'path';

export const AUTH_PROTO_PATH = join(
  process.cwd(),
  'libs/common/src/protos/auth.proto',
);
export const CATALOG_PROTO_PATH = join(
  process.cwd(),
  'libs/common/src/protos/catalog.proto',
);
export const CART_PROTO_PATH = join(
  process.cwd(),
  'libs/common/src/protos/cart.proto',
);
